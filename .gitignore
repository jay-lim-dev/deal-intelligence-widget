
.DS_Store
node_modules/
 
# Zoho widget — NEVER commit these
app/variables.json      # Custom Variable values — treat like a .env file
.zet/                   # Local zet cache and session data
*.zip                   # Packed widget files — generated by zet pack 